---
import { Image, Picture } from "@astrojs/image/components";
const nums = Astro.props.nums;
---

<div>
  {
    nums.map((num) => (
      <Image
        src={`/images/awards/${num}.png`}
        alt="Greenfingers film award"
        format="webp"
        width={1735}
        height={1152}
      />
    ))
  }
</div>

<style>
  div {
    margin-top: 8rem;
    display: flex;
    width: 100%;
    gap: 4rem;
    align-items: center;
    justify-content: center;
  }

  img {
    max-width: 6rem;
    opacity: 0.2;
  }
</style>
