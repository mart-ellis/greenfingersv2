---
---

<section>
  <video playsinline autoplay muted loop>
    <source src="/videos/neigh.mp4" type="video/mp4" />
  </video>
  <div>
    <h1>Green<br />fingers</h1>
    <p>Your friendly <span>award-winning</span> film collective.</p>
  </div>
</section>

<style>
  section {
    height: 100vh;
    background: var(--dark-green);
    display: flex;
    padding: 35% var(--section-padding);
    position: relative;
  }

  video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(60%);
  }

  div {
    z-index: 10;
  }

  h1 {
    font-family: var(--humane);
    text-transform: uppercase;
    font-size: 52vw;
    line-height: 38vw;
  }

  p {
    text-transform: uppercase;
    font-weight: 300;
    font-size: 0.8rem;
    letter-spacing: var(--tracking-tight);
  }

  span {
    font-weight: 600;
  }

  @media (min-width: 768px) {
    h1 {
      font-size: 36vw;
    }

    br {
      display: none;
    }
  }
</style>
